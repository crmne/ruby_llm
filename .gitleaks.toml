[extend]
useDefault = true

[[allowlists]]
description = "Model pagination nextPageToken fields in model-refresh fixtures"
regexTarget = "match"
paths = [
  '''spec/fixtures/vcr_cassettes/models_.*\.yml$'''
]
regexes = [
  '''"nextPageToken":\s*"[^"]+"'''
]
